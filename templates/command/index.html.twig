{% extends 'base_3.html.twig' %}

{% block body %}

    <main class="pt-3 pt-md-0">
        <div class="container">
            <h1 class="text-center text-uppercase mb-3">gestion des commandes</h1>

            <section class="card">
                <div class="card-header bg-warning text-white font-poppins">
                    <h3 class="text-capitalize mb-0"><i class="fas fa-tasks mr-1"></i>commandes</h3>
                </div>
                <div class="card-body table-responsive">
                    {% include 'command/command-table.html.twig' %}
                </div>
            </section>
        </div>
    </main>

{% endblock %}